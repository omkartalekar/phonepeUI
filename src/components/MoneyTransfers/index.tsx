import IconGrid from '../IconGrid';
import { MONEY_TRANSFERS_ITEMS } from '../../constants/iconItems';

export default function MoneyTransfers() {
  return <IconGrid title="Money Transfers" items={MONEY_TRANSFERS_ITEMS} />;
}
